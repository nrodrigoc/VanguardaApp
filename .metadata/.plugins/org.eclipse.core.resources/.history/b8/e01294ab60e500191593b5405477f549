package gerenciadordefila;

import java.util.Scanner;

public class Launcher {
	
	public static void main(String[] args) {
		Contador fila = new Contador();
		
		fila.iniciarFila();
		
		while(fila.isOpen()) {
			System.out.println("Ficha atual da fila: " + fila.getFichaAtual());
			Scanner input = new Scanner(System.in);
			String s = input.next();
			
			s.toLowerCase();
			switch(s) {
				case "add": fila.adicionarPessoa(); break;
				case "proximo": fila.proximaPessoa(); break;
				case "fechar": fila.fecharFila(); break;
				default: break;
			
			}	
			
			
		}
		
		System.out.println("Fim do programa!");
		
	}
}
